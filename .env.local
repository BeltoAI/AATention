# Base URL for QR links in production (set on Vercel as well)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# Your LLM endpoint (override on Vercel if different)
LLM_URL=http://minibelto.duckdns.org:8007/v1/completions
